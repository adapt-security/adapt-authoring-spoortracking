# adapt-authoring-spoortracking

Makes course content compatible with **spoor** (the Adapt SCORM/xAPI tracking extension), ensuring built courses carry the tracking identifiers needed to report progress and completion to an LMS.

Extends `AbstractModule` from [adapt-authoring-core](../adapt-authoring-core).
